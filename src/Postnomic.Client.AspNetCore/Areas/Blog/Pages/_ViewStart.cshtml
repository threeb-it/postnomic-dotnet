@{
    // Inherit the host application's layout by defaulting to _Layout.
    // If the host uses a different layout name, it will still be resolved
    // through the normal Razor Pages view-start chain.
    Layout = "_Layout";
}
